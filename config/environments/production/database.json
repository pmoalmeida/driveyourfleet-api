{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "uri": "${process.env.DATABASE_URI || ''}",
        "host": "${process.env.DATABASE_HOST || 'ds239071.mlab.com'}",
        "port": "${process.env.DATABASE_PORT || 39071}",
        "database": "${process.env.DATABASE_NAME || 'strapi-production'}",
        "username": "${process.env.DATABASE_USERNAME || 'administrator'}",
        "password": "${process.env.DATABASE_PASSWORD || 'driveyourfleetetapa1'}"
      },
      "options": {
        "ssl": "${process.env.DATABASE_SSL || false}",
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || ''}"
      }
    }
  }
}
